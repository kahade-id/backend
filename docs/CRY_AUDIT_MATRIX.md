# CRY-001 to CRY-025 Audit Matrix

| Issue | Severity | Status | Resolution | File(s) |
|-------|----------|--------|------------|---------|
| CRY-001 | CRITICAL | Verified Fixed | No dead HMAC verification code exists. `hmacSHA256` is actively used for data integrity (NIK dedup in kyc.service, bank account dedup in bank-accounts.service). No request HMAC signing/verification middleware was found. | `.backend/src/common/utils/crypto.util.ts`, `.backend/src/modules/kyc/kyc.service.ts`, `.backend/src/modules/bank-accounts/bank-accounts.service.ts` |
| CRY-002 | HIGH | Already Fixed | Frontend HMAC fallback uses `digestRawBytes(Uint8Array)` via `ExpoCrypto.digest` — raw byte input, no double-encoding. | `lib/hmacUtils.ts` |
| CRY-003 | HIGH | Fixed Now | Consolidated to single HMAC key system. `lockState.ts` now imports shared key from `security.ts` (`getHmacKeyBytes`/`getHmacKeyHex`). Domain separation via message prefixing. `lib/api.ts` has no HMAC code. | `lib/lockState.ts`, `lib/security.ts` |
| CRY-004 | MEDIUM | Documented | AES-CTR security boundary documented: fresh random 16-byte IV per encrypt(), MAX_CACHE_ENTRIES=100, ephemeral per-session key. Collision probability negligible. | `lib/offlineCache.ts` |
| CRY-005 | MEDIUM | Already Fixed | `getHmacKeyBytes` in offlineCache concatenates raw byte arrays (encKey + label), passes to `Crypto.digest` with raw byte input. | `lib/offlineCache.ts:181-194` |
| CRY-006 | MEDIUM | Fixed Now | Added `clearCryptoKeys()` that zero-fills `_encryptionKeyBytes` and `_hmacKeyBytes` via `Uint8Array.fill(0)`, removes sessionStorage key on web. Called on both logout and unauthorized flows. | `lib/offlineCache.ts`, `contexts/AuthContext.tsx` |
| CRY-007 | MEDIUM | Fixed Now | `lockState.ts` now uses shared HMAC key from `security.ts` which stores in sessionStorage on web (not AsyncStorage). Key never stored alongside protected data. | `lib/lockState.ts`, `lib/security.ts` |
| CRY-008 | LOW | Already Correct | `constantTimeEqual` implementation is correct. Length XOR + byte-by-byte comparison with null padding is standard. No action needed per audit. | `lib/offlineCache.ts:201-208` |
| CRY-009 | HIGH | Already Fixed | Wallet PIN pepper validated in constructor, throws if missing: `if (!pepper) throw new Error(...)`. | `.backend/src/modules/wallet/wallet.service.ts:93-97` |
| CRY-010 | MEDIUM | Already Fixed | Uses `hmacPinDigest(pepper, pin)` (HMAC-SHA256 with pepper as key, pin as message) as bcrypt input. Legacy `pepper + pin` has migration path. | `.backend/src/modules/wallet/wallet.service.ts:542-554`, `.backend/src/common/utils/crypto.util.ts:201-203` |
| CRY-011 | LOW | Already Fixed | Dummy PIN hash initialized in `onModuleInit()` (async, at startup). `getDummyPinHash()` throws if not initialized. | `.backend/src/modules/wallet/wallet.service.ts:100-102, 524-529` |
| CRY-012 | MEDIUM | Documented | Salt rotation implemented via `rotateSalt()` with versioned salt system (`CURRENT_SALT_VERSION`). `clearEncryptionKey()` called on logout. | `lib/tokenEncryption.ts:306-324, 272-304` |
| CRY-013 | LOW | Documented | Web encryption key stored in sessionStorage — intentionally ephemeral. Cleared on tab close. Cache from previous sessions is undecryptable by design. | `lib/offlineCache.ts:149-163` |
| CRY-014 | MEDIUM | Already Fixed | `requireBiometricGate()` called before `getEncryptionKeyBytes()`. Requires biometric/device-credential auth on mobile. Web bypasses (session-scoped key). | `lib/offlineCache.ts:113-145` |
| CRY-015 | LOW | Already Fixed | NIK hashing uses `argon2HashNik()` with argon2id (memoryCost=65536, timeCost=3). Legacy `hmacSHA256(nik)` kept for backward-compatible lookups only. | `.backend/src/common/utils/crypto.util.ts:229-240`, `.backend/src/modules/kyc/kyc.service.ts:104-105` |
| CRY-016 | MEDIUM | Already Fixed | Distinct KYC encryption keys: `_kycNikEncryptionKey`, `_kycKtpEncryptionKey`, `_kycSelfieEncryptionKey`. Production validates all three are set and distinct. | `.backend/src/common/utils/crypto.util.ts:18-20, 40-47` |
| CRY-017 | LOW | Already Fixed | TOTP secrets encrypted with `encryptAES(secret.base32)` before database storage. Decrypted only when needed for verification. | `.backend/src/modules/auth/auth.service.ts:1206, 810, 1251, 1311` |
| CRY-018 | MEDIUM | Documented | Midtrans signature format `SHA-512(orderId+statusCode+grossAmount+serverKey)` is mandated by Midtrans API. Constant-time comparison via `timingSafeEqual`. | `.backend/src/modules/payment/payment.service.ts:170-193` |
| CRY-019 | LOW | Already Fixed | Bcrypt rounds configurable via `BCRYPT_ROUNDS` env var with minimum floor of 10. Both `app.constants.ts` and `crypto.util.ts` support runtime configuration. | `.backend/src/common/constants/app.constants.ts:14-18`, `.backend/src/common/utils/crypto.util.ts:49-51` |
| CRY-020 | MEDIUM | Already Fixed | On successful login, `extractBcryptRounds()` checks stored hash rounds. If below current config, password is re-hashed with current rounds. | `.backend/src/modules/auth/auth.service.ts:682-687, 1752-1755` |
| CRY-021 | LOW | Fixed Now | Referral code increased from 6 to 8 random chars (`KH` + 8 from `[A-Z0-9]`). Entropy: 36^8 ≈ 2.8 trillion ≈ 41.4 bits. Validators accept 6-8 for backward compatibility. | `.backend/src/common/utils/id-generator.util.ts:68-69`, `.backend/src/modules/referral/dto/apply-referral.dto.ts`, `app/(auth)/complete-profile.tsx` |
| CRY-022 | MEDIUM | Already Fixed | `computeHmac` in offlineCache accepts `Uint8Array` key and data. Calls `hmacSha256Bytes(keyHex, data)` which imports key as raw bytes and operates on raw data. | `lib/offlineCache.ts:196-199`, `lib/hmacUtils.ts:77-116` |
| CRY-023 | LOW | Verified Correct | All Iris API URLs use `https://` (hardcoded in `getIrisBaseUrl`). Basic Auth key is protected by TLS in transit. | `.backend/src/modules/payment/midtrans.service.ts:276-294` |
| CRY-024 | LOW | Documented | Backend uses centralized `secureRandom()` helper in `id-generator.util.ts` built on `crypto.randomBytes` (CSPRNG). All ID/token generation uses this. | `.backend/src/common/utils/id-generator.util.ts` |
| CRY-025 | INFO | Documented | Keys managed via env vars. KMS integration (AWS/GCP/Vault) documented as future infrastructure work. Out of scope for this task. | `.backend/src/common/utils/crypto.util.ts` |
